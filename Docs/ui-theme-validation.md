# 视觉改造验收记录

日期：2026-09-20。仅在 floral_notepaper_regulusapplex 内实施本轮改动，保留先前的未提交功能开发。

## 本轮结果

- 侧栏标题为“笔记 / 筆記 / Notes”，隐藏根节点。标题可返回默认位置、清空搜索，也接受将笔记拖回默认位置。
- 四个系统目录固定在前，与自定义目录共用行样式；文章改为文件图标、标题、记录日期、字数和单行摘要的轻量列表。
- 日记直接展示 ISO 周，周小结直接展示月份，月报展示年份。隐藏的年度层只影响展示，真实文件路径、metadata 和笔记 ID 未改变。
- 文件夹选择、展开和文章选择相互独立；未加入自动打开最新文章或自动创建文章的行为。已有启动时打开首篇的行为保持不变。
- 刷新与新建目录合并至标题旁的图标，悬停和键盘聚焦时增强显示。空目录选中后显示提示。
- blog 的两套颜色通过共享语义变量用于主窗口、编辑预览、设置和弹窗；系统磁贴色也跟随新主题，自定义磁贴色保持不变。
- 主窗口四角半径 11px，裁剪为 hidden；侧栏仍为默认 280px，窗口大小和编辑区分栏方式保持原有机制。自定义背景沿用原背景图和遮罩设置。
- 未复制视觉稿或插画，字体与图标相对 floral-notepaper-main 的文件哈希差异数为 0。

## 自动化验证

- 前端测试：30 个文件、165 项通过。
- Rust 测试：165 项通过，另有主程序与文档测试各 0 项通过。测试进程设置 NO_PROXY=127.0.0.1,localhost。
- TypeScript 类型检查、oxlint、修改文件的 oxfmt 检查与 git diff --check 通过。
- 新增交互测试覆盖：三语言时间分组、ISO 跨年、补记按记录日期排序、隐藏根节点且保留根下文章、空目录、选择与展开互不干扰、右键、拖拽与默认位置入口。
- Windows x64 NSIS 构建通过，版本保持 1.2.0。安装包未签名。

## 实际渲染检查

使用打包后的 dist，在 Edge/Chromium 中加载实际应用组件和样式，接入独立的 IPC 测试适配器。测试笔记只存在于该浏览器上下文的测试存储中，未写入正式笔记目录。

- 检查浅色和深色配色、11px 四角、280px 侧栏、日记展开、文章加载与 Markdown 预览。
- 检查文件夹选择和折叠不切换文章，手动保存后重新加载仍能取回正文。
- 检查三种语言的标题和周分组。
- 检查标题入口清空搜索，以及 900×620 最小窗口没有水平溢出。
- 捕获的页面运行时异常数为 0。

截图（实际前端渲染，测试内容）：

- [浅色](../local-build/visual-review-20260920/light.png)
- [深色](../local-build/visual-review-20260920/dark.png)
- [最小窗口](../local-build/visual-review-20260920/minimum-window.png)

测试脚本保留在 local-build/visual-review-20260920/review.cjs。该目录由现有 .gitignore 排除。

## 产物与边界

大小：19,166,989 bytes。SHA-256：139057615ABE54D10541294F314FB44BDF4994B0421784606504E4DFD632C4A1。

安装包：src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/花笺\_1.2.0_x64-setup.exe。

本轮完成构建，未执行安装或替换已安装版本。曾使用独立配置、数据和 WebView 目录启动构建后的原生程序，并已关闭；该运行时未开放调试接口，因此以上交互验证是生产前端加 IPC 适配器的验证，不能视作安装版的完整桌面回归。真实文件保存、周期唯一性、移动和恢复由 Rust 测试覆盖。

原版程序 SHA-256 仍为 A049C4084D399FD326E62E97D4C5095225B96DFF4050725BA4432A5D99A67FCD；原版进程未关闭。没有修改两个参考项目、真实笔记数据、存储目录、安装标识或 Rust 文件操作实现。没有 Git 初始化、提交、推送或发布。

构建沿用原工程缺少 SourceHanSerifSC-Bold.woff2 的警告；没有补换原字体资源，不影响本次打包成功。
